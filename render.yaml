services:
  - type: web
    name: cad-viewer-backend
    env: docker
    region: singapore
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    plan: free
    envVars:
      - key: PORT
        value: 8000
